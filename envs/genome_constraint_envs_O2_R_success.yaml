name: genome_constraint_envs_O2_R

channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # Core language (from O2 modules)
  # NOTE: Before using this environment, load the O2 modules:
  #   module load conda/miniforge3/24.11.3-0
  #   module load gcc/14.2.0
  #   module load cuda/12.8
  - r-base=4.4.2

  # Core R packages (successfully installed)
  - r-tidyverse
  - r-ggplot2
  # Note: r-dplyr, r-tidyr, r-readr, r-data.table are included in r-tidyverse

  # Visualization (successfully installed)
  - r-plotly
  - r-ggpubr

  # Bioinformatics (Bioconductor) - successfully installed
  - bioconductor-biostrings
  - bioconductor-genomicranges
  # Note: bioconductor-rtracklayer installed but has R version compatibility issues

  # Phylogeny and evolution (successfully installed)
  - r-ape
  - r-phytools

  # Genome size and constraint analysis (successfully installed)
  - r-phylolm
  - r-caper
  - r-picante
  - r-geiger

  # Utilities (successfully installed)
  - r-rmarkdown
  - r-knitr

  # Statistical modeling (successfully installed)
  - r-lme4
  - r-mgcv
  # Note: r-caret installed but has R version compatibility issues (fails to load)

  # Packages skipped or failed:
  # - bioconductor-rtracklayer (R version compatibility issue - installed but fails to load)
  # - bioconductor-genomicfeatures (may depend on rtracklayer)
  # - bioconductor-clusterprofiler (R version compatibility issue - installed but fails to load)
  # - bioconductor-pathview (may depend on clusterProfiler)
  # - r-caret (R version compatibility issue - installed but fails to load)

  # Successfully installed packages will be added below:

